<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sách Online - Thư viện E-Book</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Thiết lập font Inter */
        html {
            font-family: 'Inter', sans-serif;
        }

        /* Chỉ giữ lại các định nghĩa CSS thuần túy nếu cần */
        /* Các class Tailwind đã được đưa trực tiếp vào thẻ HTML */
        .book-cover-wrapper {
            /* Giúp kiểm soát kích thước bìa sách */
            height: 12rem;
            /* h-48 */
        }

        @media (min-width: 640px) {

            /* sm breakpoint */
            .book-cover-wrapper {
                height: 16rem;
                /* sm:h-64 */
            }
        }
    </style>
</head>

<body class="bg-gray-50">
    <header class="bg-gray-900 text-white shadow-lg sticky top-0 z-10">
        <div class="flex items-center justify-between max-w-7xl mx-auto p-4">
            <a href="/" class="text-2xl font-bold text-yellow-400">ArvaLibrary</a>

            <div class="flex-grow max-w-md mx-4 hidden md:block">
                <form action="/search" method="GET" class="flex">
                    <input type="text" name="q" placeholder="Tìm kiếm..."
                        class="w-full p-2 text-sm text-gray-900 border border-gray-300 rounded-l-lg focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-yellow-500"
                        required>
                    <button type="submit"
                        class="p-2 text-sm font-medium text-white bg-yellow-600 rounded-r-lg hover:bg-yellow-700 focus:outline-none focus:ring-2 focus:ring-yellow-500 transition duration-200">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                            xmlns="http://www.w3.org/2000/svg">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                        </svg>
                    </button>
                </form>
            </div>

            <div class="flex items-center space-x-2">
                <a href="/contact" class="hidden sm:inline-block">
                    <button
                        class="text-sm font-medium py-2 px-4 rounded-lg flex items-center transition duration-200 bg-yellow-700 hover:bg-yellow-800 whitespace-nowrap">
                        Liên hệ
                    </button>
                </a>
            </div>
        </div>
    </header>

    <main class="max-w-7xl mx-auto p-4 sm:p-6">

        <% if (typeof message !=='undefined' && message) { %>
            <div
                class="p-4 my-4 rounded-lg font-medium <%= (messageType === 'success') ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
                <%= message %>
            </div>
            <% } %>

                <% const renderBookCard=(book)=> { %>
                    <a href="/book/<%= book.id %>">
                        <div
                            class="book-card shadow-lg rounded-lg bg-white transition duration-300 hover:shadow-2xl hover:scale-[1.02] cursor-pointer">
                            <div class="book-cover-wrapper w-full overflow-hidden rounded-t-lg">
                                <img class="w-full h-full object-cover" src="<%= book.cover_url %>"
                                    alt="<%= book.title %>"
                                    onerror="this.onerror=null; this.src='https://placehold.co/300x400/94a3b8/ffffff?text=B%C3%ACa+s%C3%A1ch+khuy%E1%BA%BFt'">
                            </div>

                            <div class="p-2">
                                <p class="book-title text-base font-bold truncate text-gray-800"
                                    title="<%= book.title %>">
                                    <%= book.title %>
                                </p>
                                <div class="flex justify-between items-center text-xs mt-1">
                                    <span class="text-gray-500 truncate">
                                        <%= book.author || 'Tác giả ẩn danh' %>
                                    </span>
                                    <span class="font-bold text-yellow-600 flex items-center">
                                        <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                            <path
                                                d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.23 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.95-.69l1.07-3.292z">
                                            </path>
                                        </svg>
                                        <%= book.rating %>
                                    </span>
                                </div>
                            </div>
                        </div>
                    </a>
                    <% } %>


                        <h2 class="text-3xl font-extrabold text-gray-900 mt-8 mb-4 border-b pb-2">
                            Bảng Xếp Hạng <span class="text-yellow-600"></span>
                        </h2>
                        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4 p-4">
                            <% if (typeof rankingBooks !=='undefined' && rankingBooks.length> 0) { %>
                                <% rankingBooks.forEach(renderBookCard) %>
                                    <% } else { %>
                                        <p class="col-span-full text-center text-gray-500 p-8">Chưa có sách nào trong
                                            Bảng xếp hạng.</p>
                                        <% } %>
                        </div>

                        <h2 class="text-3xl font-extrabold text-gray-900 mt-12 mb-4 border-b pb-2">
                            Sách <span class="text-gray-500"></span>
                        </h2>
                        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4 p-4">
                            <% if (typeof generalBooks !=='undefined' && generalBooks.length> 0) { %>
                                <% generalBooks.forEach(renderBookCard) %>
                                    <% } else { %>
                                        <p class="col-span-full text-center text-gray-500 p-8">Chưa có sách nào trong
                                            mục Sách.</p>
                                        <% } %>
                        </div>

                        <h2 class="text-3xl font-extrabold text-gray-900 mt-12 mb-4 border-b pb-2">
                            Truyện <span class="text-gray-500"></span>
                        </h2>
                        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4 p-4">
                            <% if (typeof stories !=='undefined' && stories.length> 0) { %>
                                <% stories.forEach(renderBookCard) %>
                                    <% } else { %>
                                        <p class="col-span-full text-center text-gray-500 p-8">Chưa có truyện nào trong
                                            mục Truyện.</p>
                                        <% } %>
                        </div>

    </main>

    <footer class="bg-gray-900 text-gray-300 mt-12 border-t border-yellow-700">
        <div class="max-w-7xl mx-auto p-6 grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12">

            <div class="md:col-span-1">
                <p class="text-3xl font-extrabold text-yellow-400 mb-2 tracking-wider">ArvaLibrary</p>
                <p class="text-base text-gray-400">Thư viện sách, truyện online hàng đầu Việt Nam.</p>
            </div>

            <div class="md:col-span-1 text-sm space-y-3">
                <p class="text-lg font-semibold text-white mb-3 border-b border-gray-700 pb-1">Thông tin liên hệ</p>

                <p class="flex items-start">
                    <svg class="w-5 h-5 mr-3 mt-0.5 text-yellow-500 flex-shrink-0" fill="none" stroke="currentColor"
                        viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.828 0l-4.243-4.243a8 8 0 1111.314 0z">
                        </path>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    </svg>
                    <span class="text-gray-400">Địa chỉ: 123A, BCD, phường HKJ, Tphcm</span>
                </p>

                <a href="mailto:arvalibrary@gamil.com" class="flex items-center hover:text-yellow-400 transition">
                    <svg class="w-5 h-5 mr-3 text-yellow-500 flex-shrink-0" fill="none" stroke="currentColor"
                        viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z">
                        </path>
                    </svg>
                    <span class="text-gray-400">Email: arvalibrary@gamil.com</span>
                </a>

                <a href="tel:0123456789" class="flex items-center hover:text-yellow-400 transition">
                    <svg class="w-5 h-5 mr-3 text-yellow-500 flex-shrink-0" fill="none" stroke="currentColor"
                        viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z">
                        </path>
                    </svg>
                    <span class="text-gray-400">Hotline (Zalo): 0123456789</span>
                </a>
            </div>

            <div class="md:col-span-1 text-sm space-y-2">
                <p class="text-lg font-semibold text-white mb-3 border-b border-gray-700 pb-1">Liên kết nhanh</p>
                <ul class="space-y-2">
                    <li><a href="/" class="text-gray-400 hover:text-yellow-400 transition">Trang chủ</a></li>
                    <li><a href="/Contact" class="text-gray-400 hover:text-yellow-400 transition">Liên Hệ</a></li>

                </ul>
            </div>
        </div>

    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Logic Javascript (nếu cần)
            console.log('Trang chủ ArvaLibrary đã tải xong.');
        });
    </script>
</body>

</html>