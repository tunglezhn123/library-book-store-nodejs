<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> - Chi Tiết Sách | ArvaLibrary
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        html {
            font-family: 'Inter', sans-serif;
        }

        /* Chiều cao cố định cho bìa sách trên trang chi tiết */
        .detail-cover {
            width: 100%;
            height: 24rem;
            /* h-96 */
            max-height: 400px;
        }

        @media (min-width: 768px) {
            .detail-cover {
                width: 250px;
                height: 350px;
            }
        }
    </style>
</head>

<body class="bg-gray-50">
    <header class="bg-gray-900 text-white shadow-lg sticky top-0 z-10">
        <div class="flex items-center justify-between max-w-7xl mx-auto p-4">
            <a href="/" class="text-2xl font-bold text-yellow-400">ArvaLibrary</a>

            <div class="flex-grow max-w-md mx-4 hidden md:block">
                <form action="/search" method="GET" class="flex">
                    <input type="text" name="q" placeholder="Tìm kiếm..."
                        class="w-full p-2 text-sm text-gray-900 border border-gray-300 rounded-l-lg focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-yellow-500"
                        required>
                    <button type="submit"
                        class="p-2 text-sm font-medium text-white bg-yellow-600 rounded-r-lg hover:bg-yellow-700 focus:outline-none focus:ring-2 focus:ring-yellow-500 transition duration-200">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                            xmlns="http://www.w3.org/2000/svg">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                        </svg>
                    </button>
                </form>
            </div>

            <div class="flex items-center space-x-2">
                <a href="/contact" class="hidden sm:inline-block">
                    <button
                        class="text-sm font-medium py-2 px-4 rounded-lg flex items-center transition duration-200 bg-yellow-700 hover:bg-yellow-800 whitespace-nowrap">
                        Liên hệ
                    </button>
                </a>
            </div>
        </div>
    </header>

    <main class="max-w-7xl mx-auto p-4 sm:p-6">
        <a href="/" class="text-yellow-600 hover:text-yellow-700 flex items-center mb-6">
            <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                xmlns="http://www.w3.org/2000/svg">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18">
                </path>
            </svg>
            Quay lại
        </a>

        <div class="bg-white shadow-xl rounded-xl p-6 md:p-10 mb-10">
            <div class="flex flex-col md:flex-row gap-8">
                <div class="md:flex-shrink-0">
                    <div class="detail-cover overflow-hidden rounded-lg shadow-2xl mx-auto md:mx-0">
                        <img class="w-full h-full object-cover" src="<%= book.cover_url %>" alt="<%= book.title %>"
                            onerror="this.onerror=null; this.src='https://placehold.co/300x400/94a3b8/ffffff?text=B%C3%ACa+s%C3%A1ch+khuy%E1%BA%BFt'">
                    </div>

                    <% const readUrl=book.is_vip ? '/vip-upgrade' : `/book/${book.id}/preview`; const
                        buttonClass=book.is_vip ? 'bg-red-600 hover:bg-red-700' : 'bg-green-600 hover:bg-green-700' ;
                        const buttonText=book.is_vip ? 'Nâng Cấp VIP để Đọc' : 'Đọc Ngay' ; %>

                        <a href="<%= readUrl %>">
                            <button
                                class="mt-4 w-full md:w-[250px] py-3 text-lg font-bold text-white <%= buttonClass %> rounded-lg transition duration-300">
                                <%= buttonText %>
                            </button>
                        </a>
                </div>

                <div class="flex-grow">
                    <h1 class="text-4xl font-extrabold text-gray-900 mb-2">
                        <%= book.title %>
                    </h1>
                    <p class="text-xl text-yellow-600 font-semibold mb-3">
                        <%= book.author || 'Tác giả ẩn danh' %>
                    </p>

                    <% if (book.publication_date) { %>
                        <p class="text-lg text-gray-700 font-medium mb-4">
                            Năm xuất bản:
                            <span class="font-bold text-gray-900">
                                <%= book.publication_date %>
                            </span>
                        </p>
                        <% } %>

                            <div class="flex items-center space-x-4 mb-6 border-b pb-4">
                                <span class="text-3xl font-bold text-yellow-500 flex items-center">
                                    <svg class="w-6 h-6 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                        <path
                                            d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.23 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.95-.69l1.07-3.292z">
                                        </path>
                                    </svg>
                                    <%= book.rating %>
                                </span>
                                <span class="text-lg text-gray-500">|</span>
                                <span class="text-lg text-gray-700 font-medium">Thể loại:
                                    <span class="font-bold text-gray-900">
                                        <%= book.category %>
                                    </span>
                                </span>
                                <% if(book.is_vip) { %>
                                    <span
                                        class="inline-flex items-center px-3 py-1 bg-red-500 text-white text-xs font-bold rounded-full">
                                        VIP
                                    </span>
                                    <% } %>
                            </div>

                            <h2 class="text-2xl font-bold text-gray-800 mb-3">Giới Thiệu Sách</h2>
                            <p class="text-gray-700 leading-relaxed text-base whitespace-pre-wrap">
                                <%= book.description || 'Chưa có mô tả chi tiết cho cuốn sách này.' %>
                            </p>


                </div>
            </div>
        </div>

        <div class="bg-white shadow-xl rounded-xl p-6 md:p-10">
            <h2 class="text-2xl font-bold text-gray-800 mb-6 border-b pb-3">Bình Luận (<%= comments.length %>)</h2>

            <div class="mb-8">
                <form action="/book/<%= book.id %>/comment" method="POST">
                    <textarea name="commentText" id="commentText"
                        class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500"
                        rows="4" placeholder="Viết bình luận của bạn..." required></textarea>
                    <button type="submit"
                        class="mt-2 py-2 px-4 bg-yellow-600 text-white font-semibold rounded-lg hover:bg-yellow-700 transition duration-300">
                        Gửi Bình Luận
                    </button>
                </form>
            </div>

            <div class="space-y-4">
                <% if (comments.length> 0) { %>
                    <% comments.forEach(comment=> { %>
                        <div class="p-4 border border-gray-100 rounded-lg bg-gray-50">
                            <div class="flex justify-between items-center mb-1">
                                <span class="font-bold text-gray-800">
                                    <%= comment.user %>
                                </span>
                                <span class="text-xs text-gray-500">
                                    <%= comment.date %>
                                </span>
                            </div>
                            <p class="text-gray-700">
                                <%= comment.text %>
                            </p>
                        </div>
                        <% }) %>
                            <% } else { %>
                                <p class="text-center text-gray-500 p-4">Chưa có bình
                                    luận nào. Hãy là người đầu tiên!
                                </p>
                                <% } %>
            </div>
        </div>
    </main>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Logic Javascript (nếu cần)
            console.log('Trang chi tiết sách đã tải xong.');
        });
    </script>
</body>

</html>