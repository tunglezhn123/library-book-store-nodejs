<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        html {
            /* Sử dụng font Inter */
            font-family: 'Inter', sans-serif;
        }

        /* Thiết lập chiều cao cho wrapper bìa sách để đồng nhất */
        .book-cover-wrapper {
            height: 14rem;
            /* Tăng nhẹ chiều cao mặc định */
            position: relative;
        }

        @media (min-width: 640px) {
            .book-cover-wrapper {
                height: 18rem;
                /* Chiều cao lớn hơn trên màn hình lớn */
            }
        }

        /* Hiệu ứng cho hình ảnh bên trong wrapper */
        .book-cover-wrapper img {
            transition: transform 0.3s ease-in-out;
        }

        /* Hiệu ứng phóng to nhẹ khi hover trên cả thẻ sách */
        .book-card:hover .book-cover-wrapper img {
            transform: scale(1.05);
        }
    </style>
</head>

<body class="bg-gray-100">
    <header class="bg-gray-800 text-white shadow-xl sticky top-0 z-20">
        <div class="flex items-center justify-between max-w-7xl mx-auto p-4">
            <a href="/" class="text-3xl font-extrabold text-yellow-400 tracking-wider">ArvaLibrary</a>

            <div class="flex-grow max-w-xl mx-4 hidden md:block">
                <form action="/search" method="GET" class="flex shadow-md rounded-lg overflow-hidden">
                    <input type="text" name="q" placeholder="Tìm kiếm sách, tác giả..." value="<%= searchQuery %>"
                        class="w-full p-3 text-base text-gray-900 border-none focus:outline-none focus:ring-2 focus:ring-yellow-500"
                        required>
                    <button type="submit"
                        class="p-3 text-base font-semibold text-gray-900 bg-yellow-500 hover:bg-yellow-600 transition duration-300 flex items-center justify-center">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                            xmlns="http://www.w3.org/2000/svg">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                        </svg>
                    </button>
                </form>
            </div>

            <div class="flex items-center space-x-2">
                <a href="/contact" class="hidden sm:inline-block">
                    <button
                        class="text-sm font-medium py-2 px-4 rounded-lg flex items-center transition duration-200 bg-yellow-700 hover:bg-yellow-800 whitespace-nowrap">
                        Liên hệ
                    </button>
                </a>
            </div>
        </div>
    </header>

    <main class="max-w-7xl mx-auto p-4 sm:p-8">
        <% if (typeof message !=='undefined' && message) { %>
            <div
                class="p-4 my-6 rounded-xl font-medium shadow-md <%= (messageType === 'success') ? 'bg-green-100 text-green-800' : (messageType === 'warning' ? 'bg-yellow-100 text-yellow-800' : 'bg-red-100 text-red-800') %>">
                <%= message %>
            </div>
            <% } %>

                <h1 class="text-4xl font-extrabold text-gray-900 mt-6 mb-8">
                    Kết Quả Tìm Kiếm cho:
                    <span class="text-yellow-600 italic">"<%= searchQuery %>"</span>
                </h1>

                <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 gap-6">

                    <% const renderBookCard=(book)=> { %>
                        <div class="book-card 
                            shadow-xl rounded-xl bg-white 
                            transition duration-500 transform hover:shadow-2xl hover:-translate-y-1 
                            overflow-hidden">

                            <a href="/book/<%= book.id %>" class="block h-full">
                                <div class="book-cover-wrapper w-full overflow-hidden rounded-t-xl bg-gray-200">
                                    <img class="w-full h-full object-cover" src="<%= book.cover_url %>"
                                        alt="<%= book.title %>"
                                        onerror="this.onerror=null; this.src='https://placehold.co/300x400/94a3b8/ffffff?text=B%C3%ACa+s%C3%A1ch+khuy%E1%BA%BFt'">
                                </div>

                                <div class="p-4">
                                    <p class="book-title text-base font-bold text-gray-900 mb-1 truncate"
                                        title="<%= book.title %>">
                                        <%= book.title %>
                                    </p>

                                    <div class="flex justify-between items-center text-sm">
                                        <span class="text-gray-500 truncate italic">
                                            <%= book.author || 'Tác giả ẩn danh' %>
                                        </span>

                                        <span class="font-bold text-yellow-500 flex items-center ml-2">
                                            <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                                <path
                                                    d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.23 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.95-.69l1.07-3.292z">
                                                </path>
                                            </svg>
                                            <%= book.rating %>
                                        </span>
                                    </div>
                                </div>
                            </a>
                        </div>
                        <% } %>

                            <% if (books && books.length> 0) { %>
                                <% books.forEach(book=> { %>
                                    <%= renderBookCard(book) %>
                                        <% }) %>
                                            <% } else if (searchQuery) { %>
                                                <p
                                                    class="col-span-full text-center text-gray-500 p-12 bg-white rounded-xl shadow-lg">
                                                    😔 **Không tìm thấy** sách hoặc tác giả nào chứa từ khóa "<span
                                                        class="font-semibold text-gray-700">
                                                        <%= searchQuery %>
                                                    </span>".
                                                    Vui lòng thử lại với từ khóa khác.
                                                </p>
                                                <% } %>

                </div>
    </main>
</body>

</html>